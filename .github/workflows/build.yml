name: DriverMax APK - Herramienta Ética para Conductores

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-20.04
    timeout-minutes: 30
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3
      
    - name: Setup Java 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'adopt'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v2
      
    - name: Dar permisos a gradlew
      run: chmod +x gradlew
        
    - name: Compilar APK DriverMax
      run: |
        ./gradlew assembleDebug --stacktrace
        
    - name: Subir APK DriverMax
      uses: actions/upload-artifact@v3
      with:
        name: DriverMax-APK
        path: app/build/outputs/apk/debug/app-debug.apk 